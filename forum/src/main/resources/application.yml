spring:
  application:
    name: forum
#  data:
#    jdbc:
#      dialect: h2
  data:
    jdbc:
      dialect: mysql
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql_base_td6_forum:3306/basetd6forum
    username: usertd6
    password: passwordtd6
  cloud:
    consul:
      discovery:
        register: true
      host: ${CONSUL_HOST:localhost}
      port: ${CONSUL_PORT:8500}
  rabbitmq:
    host: rabbitmq
    password: guest
    port: 5672
    username: guest
    exchange: user.exchange
    queue: user.queue
    subscribe_routingkey: user.subscribe.routingkey
    unsubscribe_routingkey: user.unsubscribe.routingkey


mySecretKey : MySuperSecretKeyForJWTsguguofterdfyulioluktydfyiuofdtrsdfuliguydsrtfuftrsesj

